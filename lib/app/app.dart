import 'package:flutter/material.dart';

import '../elective2.dart';

class Elective2App extends StatelessWidget {
  const Elective2App({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Elective2(),
    );
  }
}
